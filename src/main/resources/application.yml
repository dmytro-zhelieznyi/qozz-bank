server:
  port: 8080

spring:
  application:
    name: QozzBank
  datasource:
    url: jdbc:postgresql://localhost:5432/qozzbank
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true