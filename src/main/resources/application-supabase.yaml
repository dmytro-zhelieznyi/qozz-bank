server:
  port: 8080

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    url: /api/openapi.yaml
    disable-swagger-default-url: true

supabase:
  anon-key: "sb_publishable_CP-r655jJdDbdCTvcYT0VA_SomHKjh7"

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://qhjousemoakgjegoqikh.supabase.co/auth/v1/.well-known/jwks.json
          issuer-uri: https://qhjousemoakgjegoqikh.supabase.co/auth/v1
  application:
    name: QozzBank
  # Enable Virtual Threads for high-throughput request processing
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://aws-1-eu-north-1.pooler.supabase.com:5432/postgres
    username: postgres.qhjousemoakgjegoqikh
    password: QozzBank1!@
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

# Spring Boot Actuator: Internal monitoring and system status
management:
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
    build:
      enabled: true
    git:
      mode: full
  endpoints:
    web:
      base-path: /actuator
      exposure:
        # Explicitly listing critical endpoints for bank monitoring
        include: [ "health", "info", "metrics", "prometheus", "loggers" ]
  endpoint:
    health:
      # Show detailed status of DB, Disk, and Flyway
      show-details: always
      probes:
        enabled: true
  health:
    db:
      enabled: true
    flyway:
      enabled: true

# Custom application info for /actuator/info
info:
  app:
    name: ${spring.application.name}
    description: "High-performance Core Banking System"
    version: "1.0.0"

logging:
  level:
    root: INFO
    io.qozz.qozzbank: INFO # Поправил пакет на твой io.qozz
    org.hibernate.SQL: INFO
#    org.springframework.security: DEBUG
#    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} %-5level [%25.25thread] %logger{36}: %msg%n"