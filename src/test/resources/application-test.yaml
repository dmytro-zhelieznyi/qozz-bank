spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false


  rabbitmq:
    connection-timeout: 2s
    dynamic: true


logging:
  level:
    io.qozz.qozzbank: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.transaction.interceptor: TRACE